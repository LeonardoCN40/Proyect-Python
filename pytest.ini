[tool:pytest]
markers =
    sandbox: tests que ejecutan en el sandbox de Free Range Testers
    smoke: tests básicos de verificación rápida  
    parametrized: tests que usan datos parametrizados desde CSV
    regression: tests de regresión completos
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short